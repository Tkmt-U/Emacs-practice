RET --> return
  returnキーはreturnであってenterじゃない

C-v 次の画面に進む
M-v 前の画面に戻る
C-l 画面を書き直す。このとき、カーソルのある行が画面の中央にくるようにする。（C-1 じゃなくって C-L ですよ！）

C-f 文字次に進む
C-b 文字前に戻る

M-f 単語次に進む
M-b 単語前に戻る

C-n 次の行に移動
C-p 前の行に移動

C-a 行頭に移動
C-e 行末に移動

M-a 文頭に移動
M-e 文末に移動

M-< ファイルの先頭
M-> ファイルの末尾

C-u * 数値引数を与える
  例: C-u 8 C-f 8文字先に移動
      C-u 8 * ******が挿入

C-g コマンドの取り消し

  delete 削除
  kill 消去

<deleteキー> カーソルの直前の文字を削除（delete）
C-d	カーソルのある文字を削除（delete）

M-<deleteキー>  カーソルの直前の単語を消去（kill）
M-d	     カーソル位置の後ろにある単語を消去（kill）

C-k	     カーソル位置から行末までを消去（kill）
M-k	     カーソル位置から文末までを消去（kill)

C-@ カーソルをマーク C-w 選択した部分の消去

C-y 最後に消去した文字を挿入（再入）
  M-y 過去に消去した文字を挿入（何度もタイプすると、さらに前に消去した文章を呼び戻せる）

C-x u もしくは C-_ 取り消し（undo）

  拡張コマンド
  C-x 文字による拡張
  M-x 名前による拡張

M-; コメントアウト

C-x C-f foo fooというファイルを作成
C-x C-s ファイルのセーブ
M-x recover file<returnキー> 自動セーブされた編集を回復

C-x b foo fooというバッファに移動

M-x repl s<returnキー>置換前の文字<returnキー>置換後の文字<returnキー>

M-x ***-mode ***モードに切り替える

C-o 改行

C-s カーソルの位置以降を検索
C-r カードルの位置以前を検索
C-M-s カーソルの位置以降を正規表現で検索

M-% 置換

C-x 1 ウィンドウを１つにする(つまり他のウィンドウを全部消す)
C-x 0 カレントウインドウを消す
C-x * ウインドウを*個に分割
C-M-v 他のウインドウをスクロール
C-x o 他のウインドウにカーソルを移動
C-x 4 C-f 入力したファイルを作成し、分割したウインドウに表示

M-x make-frame 新しいフレームの作成
M-x delete-frame 選択しているフレームの消去

C-h helpの表示

C-x C-c Emacsの終了
